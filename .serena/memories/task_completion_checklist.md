After code tasks: run the most relevant Gradle verification for the affected area from CxPlayer/ (at minimum build or tests for touched module). For specification/planning tasks: update .specify/feature.json, create required spec/checklist artifacts, and keep outputs aligned with _docs/plans and .specify templates. Prefer device/emulator validation for player behavior because project docs recommend real device testing for video playback and gestures.